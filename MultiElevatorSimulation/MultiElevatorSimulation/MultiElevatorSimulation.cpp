// MultiElevatorSimulation.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>

int main()
{
    std::cout << "Hello MultiElevatorSimulation Elevator !\n";
    /*
    Building building = new Building(Integer, Integer.parseInt(args[1]), Integer.parseInt(args[2]));
    building->setAlgorithm(tempAlgorithm); // Sets algorithm in Building class - Round-Robin is supported
    building->getController().setAlgorithm(tempAlgorithm); // Sets algorithm in GroupElevatorController class

    // Create N number of Floor objects
    building->createFloors();

    // Create L number of Elevator objects
    building->createElevators();

    // Start the GroupElevatorController thread
    new Thread(building.getController()).start();

    // Generate a passenger on one of the floors
    building.generatePassenger(building.getN());
    */
}

