#pragma once
#include <iostream>
using namespace std;
class Comparator {
  public :
    Comparator() {}
    Comparator(int *a, int *b) {
    //TODO
    }

    //// One function works for all data types.  This would work
    //// even for user defined types if operator '>' is overloaded
    //template <typename T> T Comparator(T x, T y)
    //{
    //    return (x > y) ? x : y;
    //}
};
